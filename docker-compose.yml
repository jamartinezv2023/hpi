version: '3.9'

services:
  postgres:
    image: postgres:16
    container_name: hpi-postgres
    environment:
      POSTGRES_DB: hpi
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
