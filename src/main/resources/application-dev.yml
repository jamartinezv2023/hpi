spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hpi
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    url: jdbc:postgresql://localhost:5432/hpi
    user: postgres
    password: postgres
